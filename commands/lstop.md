---
description: Stop all loops in the current session (alias for /loop stop all)
agent: build
---

Call the `loop_stop` tool with `{ "all": true }`. Report the result verbatim — it will say how many loops were stopped. If no loops were active, it will say "Stopped 0 loops" — that's fine, just confirm there were none running.
